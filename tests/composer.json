{
    "name": "kirjuri/tests",
    "description": "Test suite for Kirjuri. Kept apart from the application's composer.json so the committed vendor/ folder stays free of development dependencies.",
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "config": {
        "platform": {
            "php": "8.1.0"
        }
    }
}
